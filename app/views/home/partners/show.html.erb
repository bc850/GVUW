<% provide(:page_title, @partner.name) %>
<% provide(:partner_active, "active") %>

<div class="row d-flex justify-content-center mb-3">
  <div class="container">
    <div class="row">
      <div class="col-md-8 col-md-offset-2">
        <div class="card" style="width: 100%;">
          <div class="card-block">
            <div class="card-title ml-3 mt-2">
              <h1><%= @partner.name %></h1>
            </div>
          </div>
          <ul class="list-group list-group-flush">
            <li class="list-group-item">
              <p class="published mt-3"><%= @partner.display_day_published %></p>
            </li>
            <li class="list-group-item">
              <div class="event_body mt-3">
                <div class="banner_image">
                  <%= image_tag(@partner.logo) %>
                </div>
                <br>
                <div>
                  <%= @partner.body.html_safe %>
                </div>
              </div>
            </li>
            <li class="list-group-item">
              <p class="tags mt-3"><%= raw @partner.tags.map { |t| link_to t.name, partners_path(tag: t.name) }.join(', ') %></p>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</div>
