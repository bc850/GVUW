<% provide(:page_title, "All Annual Campaigns") %>
<% provide(:campaign_active, "active") %>

<div class="row">
  <%= render @campaigns %>
</div>
<div class="apple_pagination">
  <%= will_paginate @campaigns, container: false %>
</div>
