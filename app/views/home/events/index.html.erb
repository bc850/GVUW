<% provide(:page_title, "All Events") %>
<% provide(:event_active, "active") %>

<div class="overlay container d-flex justify-content-center mb-4">
  <div id="navi-parent">
    <div id="navi">
      <h2>Events</h2>
    </div>
  </div>
</div>
<div class="row">
  <%= render @events %>
</div>
<div class="apple_pagination mb-3">
  <%= will_paginate @events, container: false %>
</div>
