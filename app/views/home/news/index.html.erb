<% provide(:page_title, "All News Posts") %>
<% provide(:blog_active, "active") %>

<div class="overlay container d-flex justify-content-center mb-4">
  <div id="navi-parent">
    <div id="navi">
      <h2>News</h2>
    </div>
  </div>
</div>
<div class="row">
  <%= render @news %>
</div>
<div class="apple_pagination mb-3">
  <%= will_paginate @news, container: false %>
</div>
